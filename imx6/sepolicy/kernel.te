allow kernel self:capability { mknod };
allow kernel fuse:file rw_file_perms;
allow kernel fuse:dir rw_dir_perms;
allow kernel device:blk_file  { create setattr getattr unlink };
allow kernel device:dir  { add_name write remove_name create rmdir };
allow kernel device:chr_file  { create setattr getattr unlink };
allow kernel mediaprovider:fd use;
